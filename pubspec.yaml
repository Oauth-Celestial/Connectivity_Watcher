name: connectivity_watcher
description: A Flutter package for fast internet checks with subsecond response times and customizable offline UI.
version: 3.0.4
repository: "https://github.com/Oauth-Celestial/Connectivity_Watcher.git"

environment:
  sdk: ">=2.19.6 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.8.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  assets:
    - assets/
screenshots:
  - description: Connectivity Watcher
    path: assets/connection.png
platforms:
  android:
  ios:
  macos:
  windows:
